#!/bin/sh

set -e
set -u

target=target/checksums/$(basename $TARGET_BUNDLE)

mkdir -p $(dirname $target)

sha256sum ${TARGET_BUNDLE} | cut -d " " -f 1 > $target.sha256
sha512sum ${TARGET_BUNDLE} | cut -d " " -f 1 > $target.sha512
