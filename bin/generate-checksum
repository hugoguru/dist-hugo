#!/bin/sh

set -e
set -u

target=target/hugo-${HUGO_TYPE}-${HUGO_VERSION}-${HUGO_ARCH}.tar.gz.sha256

sha256sum ${TARGET_BUNDLE} | cut -d " " -f 1 > $target

if [ "${GITHUB_ENV:-}" != "" ]; then
    echo "TARGET_CHECKSUM=$target" >> $GITHUB_ENV
fi